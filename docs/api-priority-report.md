# API 개선 우선순위 리포트

> 대상: 세일즈맵 API·백엔드 팀 / 목적: MCP(AI 연동) 관점에서 필요한 API 개선을 **우선순위와 실사용 근거**로 보고
> 원장: [salesmap-api-issues.md](salesmap-api-issues.md) — 각 항목은 원장 이슈를 참조하되, **원장의 기술 상세를 그대로 담고 그 위에 로그 정량·VOC·레퍼런스 조사로 보강한 확장본**. 원장의 축약이 아니라 원장 이상으로 자세해야 함 (우선순위 판단에 필요한 근거를 여기서 다 볼 수 있게)
> 근거 데이터: MCP 텔레메트리 (베타 워크스페이스 ~77개, 2026-06-01~07-23, 24K+ 호출)
> 최신화: 2026-07-23 / 우선순위 판정: 초안(AI) — 확정 전

---

## 우선순위 기준

| 등급 | 기준 |
|---|---|
| **P0 (원천 불가)** | API로 **아예 불가능**. 로그에서 업무 플로우가 끊긴 게 다수 확인되거나 고객 직접 VOC |
| **P1 (심각한 비효율)** | 가능은 하나 심각한 타격 (예: 배치 없어 느리지만 되긴 함). 실사용에 실질 피해 |
| **P2 (우회+소폭 비효율)** | MCP 레이어에서 우회. 내부 비효율은 있으나 작음 |
| **P3 (참고용)** | MCP로 우회되고 성능 문제도 없음. 나중에 API 설계할 때 참고할 정리 항목 |

※ 등급은 초안(AI). 담당자가 항목별로 직접 확정.

---

## 항목 템플릿 (신규 추가 시 복사)

> 원칙: 원장 이슈의 기술 상세(코드 예시·재현·우회 구현)를 **그대로 흡수**하고, 그 위에 로그 정량·VOC·레퍼런스로 보강한다. 원장보다 얇으면 안 됨.

```markdown
### [P?] 제목 — 한 줄 요약

- **원장**: #N
- **요청**: 백엔드에 원하는 것 (필요 시 스펙 예시까지)
- **문제**: 원장의 기술 상세를 흡수 (코드 예시·메커니즘 포함)
- **실사용 증거**: 로그 정량 (기간 명시) / VOC
- **레퍼런스**: 타 CRM(HubSpot·Salesforce 등) 실제 스펙 — 원문 예시 인용
- **현재 우회와 비용**: 무엇으로 버티고 있고 그 한계·비용은 뭔지 (코드 레벨까지)
- **기대 효과**: 있으면 뭐가 어떻게 달라지는지 (가능하면 숫자)
```

---

## P0 — 지금 고객이 겪는 중

### [P0] Batch Create/Update API — "레코드 N건 일괄 생성·수정"

- **원장**: #1
- **요청**: `POST /v3/object/create`·`update`에 다건 입력 (최대 100건), create는 association 동시 세팅 포함
- **실사용 증거** (create/update 전체 2,049콜 분석, 6/1~7/23):
  - **86%(1,759콜)가 "5건 이상 연속 버스트"** — 이 도구들의 실제 용도가 사실상 배치 작업
  - 최악 사례: 고객 598명에 불리언 필드 1개 켜기 = **598콜 연속, 19.8분** (Fast campus, 6/12). 3일 뒤 225건 재분류로 반복
  - 반복 업무 사례: 한 계정이 **7월에만 버스트 12회** (교육 프로그램 선발/참여 관리 — 매주 도는 정기 업무가 매번 수십 콜)
  - 임포트 사례: 회사 37건 연속 생성 4.6분, 실패 3건 (SK렌터카)
- **현재 우회와 비용**: 단건 도구 반복 호출(사용자가 분 단위 대기 + LLM 왕복 토큰 낭비) 또는 run-script 순회(50초 상한·가드레일로 쓰기엔 소극적). 버스트 동안 우리 스로틀이 워크스페이스 rate limit(100/10초)의 **~83%를 지속 점유** → 같은 워크스페이스 동료의 AI 사용까지 밀림
- **레퍼런스 — HubSpot 배치 스펙** (2026-07 공식 문서): create·update 모두 최대 100건, `{ inputs: [...] }` 형태. create는 **body에 `associations` 배열을 동봉해 "생성하면서 연결"** 가능. update는 속성만(연결 변경은 별도 association API). 세일즈맵은 연결이 관계 필드라 batch update가 관계 필드·top-level 연결 id를 수용하면 **HubSpot의 batch association까지 한 번에 커버** (별도 association API 불필요, 원장 #20 참조)
  ```
  POST /crm/v3/objects/deals/batch/create
    { inputs: [{ properties: {...}, associations: [{ to:{id}, types:[...] }] }, ...] }
  POST /crm/v3/objects/deals/batch/update
    { inputs: [{ id, properties: {...} }, ...] }
  ```
- **구현 계획**: v3 `object/create`가 전 오브젝트(딜·리드·커오)를 지원하면 `salesmap-batch-create-objects` 신규 도구로 노출 (최대 100건 생성 + association 동시 세팅이 핵심). update도 동일 패턴
- **기대 효과**: batch 100 기준 **1,759콜 → 63콜 (28배)**, 121분 → 1~2분. 쿼타 점유 문제 동시 해소. LLM 왕복 토큰(툴콜 598회 = 응답 598회 컨텍스트 누적)도 소멸

### [P0] Activity(타임라인) 배치 조회 — "레코드 N건의 활동을 한 번에"

- **원장**: #28
- **요청**: `/v3/object/read`에 activity 인라인 파라미터, 또는 다건 objectId를 받는 배치 activity 엔드포인트
- **실사용 증거** (6월 로그):
  - list-engagements가 **전 도구 중 최다 호출(6,703콜)**, `engagements→engagements` 연속 전이 6,385회 = 레코드별 N회 반복이 구조적
  - p90 **7.9초**, 5초 초과 20.9%, **429 rate limit 52건 전부 이 도구에서 발생**
- **현재 우회와 비용**: 유형 필터·limit·응답 경량화로 완화했지만(2026-07) **N레코드=N콜 구조 자체는 백엔드 확인 결과 해소 불가** (activity API가 단일 objectId만 수용)
- **기대 효과**: "리드 87건 팔로업 현황" 같은 분석이 87콜→1콜. 429·지연·쿼타 점유 동시 해소

---

## P1 — 심각한 비효율 / 구조적 문제

### [P1] 쓰기 입력 위치 이중성 — top-level vs fieldList (+연결)

- **원장**: #3
- **요청**: 뉴버전 API에서 top-level 파라미터·기본연결·커스텀연결을 **전부 fieldList로 내려 통일**. 서버가 이름→타입을 스키마로 해석(#2와 한 묶음). 클라이언트는 위치·타입 구분 없이 이름-값만 전송
- **문제 — 같은 "필드 값 쓰기"인데 위치가 3갈래**:
  ```
  POST /v2/deal
  {
    "name": "딜 이름",           ← ① top-level 전용
    "price": 50000,              ← ① top-level 전용
    "pipelineId": "uuid",        ← ① top-level 전용
    "pipelineStageId": "uuid",   ← ① top-level 전용
    "status": "In progress",     ← ① top-level 전용
    "peopleId": "uuid",          ← ② 기본연결 top-level
    "organizationId": "uuid",    ← ② 기본연결 top-level
    "fieldList": [               ← ③ 나머지 + 커스텀연결
      { "name": "담당자", "userValueId": "uuid" }
    ]
  }
  ```
  AI가 매 필드마다 "이건 어느 갈래인가"를 판단해야 함. **연결도 같은 이중성** — 기본 연결(회사·고객)은 top-level, 커스텀 연결(관계 필드)은 fieldList로 갈림. 타 CRM은 한 축 통일(Salesforce=전부 필드, HubSpot=전부 association 리소스), 세일즈맵만 혼합
- **현재 우회와 비용 (MCP가 덮지만 한계 뚜렷)**:
  1. **예외 하드코딩** — `TOP_LEVEL_ONLY = {"금액":"price", "이름":"name", "파이프라인":"pipelineId", "파이프라인 단계":"pipelineStageId", "상태":"status"}` 맵을 손으로 유지. 필드가 늘면 계속 따라가야 하는 취약한 커버
  2. **파이프라인/단계는 커버 실패** — 담당자·팀은 `getUserMap`/`getTeamMap`으로 이름→ID 자동변환되는데 파이프라인/단계는 그런 맵이 없음. AI가 `"파이프라인": "국내영업"`(이름)을 넣으면 `"ID 형식이어야 합니다. salesmap-get-pipelines로 조회하세요"` 에러. 실사용 로그에 `[refine]: 파이프라인 값은 fieldList가 아닌 파라메터` 계열 실패 반복
  3. **silent no-op — 가장 위험** — 필드를 잘못된 위치·키로 보내면 API가 **거부하지 않고 `200 OK`를 반환하면서 값만 조용히 버림**. `updatedAt`은 갱신돼서 겉보기엔 성공. 실제 관찰된 no-op 케이스:
     - top-level `ownerId`(담당자) — `200`인데 미반영. 담당자는 `fieldList`의 `userValueId`로만 변경됨
     - 고객의 top-level `email`·`phone` — `200`인데 미저장. `fieldList`의 `이메일`/`전화`로만 저장됨
     - 스키마에 없는 임의 파라미터(예: `foobar`, `amount`) — 에러 없이 조용히 무시
     
     → 에러가 안 나니 AI가 "수정 완료"로 판단하고 **사용자에게 틀린 완료 보고**. 재조회로 검증하지 않으면 데이터가 안 바뀐 걸 아무도 모름. 명시적 400 거부보다 훨씬 나쁜 실패 유형 (조용한 데이터 유실)
  4. **커버 로직 복잡** — 필드·관계·top-level이 얽혀 `resolveProperties()`가 무거워지고, 커스텀 연결만 우회되고 기본 연결(회사·고객)은 아직 미적용이라 AI가 여전히 top-level `organizationId`/`peopleId`를 써야 함
- **부분 개선 여지 (뉴버전 전까지)**: `TOP_LEVEL_ONLY`에 objectType-스코프로 `"회사"→organizationId`·`"고객"→peopleId` 추가하면 기본 연결도 properties 단일 문법으로 흡수 (코드 십수 줄). 단 근본 해결은 백엔드 통일
- **기대 효과**: 입력이 "이름-값 하나의 규칙"으로 통일 → AI 에러율·MCP 커버 복잡도 동시 감소. silent no-op 제거. ※top-level→fieldList 통합은 기존 연동을 깨는 breaking change라 뉴버전에서만

### [P1] 견적서 발행 API — "생성까지만 되고 발송을 못 함"

- **원장**: #29
- **요청**: `POST /v2/quote/{id}/publish` (공유 링크 반환) 또는 생성 시 `publish: true` 옵션
- **실사용 증거**: 실측(2026-07) — API 생성 견적서는 `공유 링크`가 null, 발행 관련 엔드포인트 전무. 발행은 GUI 전용
- **현재 우회와 비용**: 없음 — "AI가 견적서 만들어 고객에게 발송"이 **구조적으로 완결 불가**. 생성 후 사람이 GUI에서 발행+발송을 이어받는 반쪽 자동화
- **기대 효과**: 견적 플로우(생성→발행→이메일 발송)가 API 조합으로 완결 — 세일즈 핵심 시나리오의 자동화 개방

### [P1] 커스텀 오브젝트 검색 — "커오는 조건 검색이 안 됨"

- **원장**: #4-5
- **요청**: `POST /v2/object/{type}/search`(또는 v3 검색)의 커스텀 오브젝트 지원
- **실사용 증거**: 커오를 메인 워크플로우로 쓰는 팀(티켓 관리 등)이 조건 검색 대신 **전량 조회 후 클라이언트 필터**로 우회하는 패턴 관찰 (run-script `getAll` 사용례 다수)
- **현재 우회와 비용**: 전량 스캔 — 레코드가 수백 건이면 낭비, 수천 건이면 사실상 불가
- **기대 효과**: 커오 기반 업무(티켓·계약·정산)가 기본 오브젝트와 동급으로 검색 가능

### [P1] 레코드 병합 API

- **원장**: #24
- **요청**: `POST /v2/object/{type}/merge` (survivor 지정 + 연결 이전)
- **실사용 증거**: 한 고객이 회사 중복 정리를 위해 run-script **77회, 7시간**에 걸쳐 병합 로직을 자체 조립 (연결 고객·딜 재연결 + [DEL] 마킹, 2026-07-06)
- **현재 우회와 비용**: 공개 API 조합으로 가능은 하나 위험한 대량 쓰기를 매번 AI가 즉석 작성 — 실수 시 데이터 꼬임
- **기대 효과**: CRM 위생의 최다 빈도 작업(중복 정리)이 안전한 1콜로. MCP에 merge 도구로 노출 가능

### [P1] 그 외 우회 불가 (빈도·가치 재판단 대기)

| 항목 | 원장 | 우회 불가 사유 |
|---|---|---|
| 이메일 본문 미제공 | #9 | activity/email 응답에 제목·메타만 — 본문 자체가 안 와서 이메일 내용 분석 불가 |
| 필드 수정 API 부재 | #19 | 옵션 값 추가·라벨 변경이 UI 전용 — "카테고리에 옵션 추가" 자동화 차단 |

---

## P2 — 우회되지만 내부 비효율 발생

### [P2] fieldList 타입 키 패턴 — 값마다 타입별 키를 골라야 함

- **원장**: #2
- **요청**: 프로퍼티 **이름-값만** 받고 서버가 스키마로 타입을 해석 (HubSpot식 평탄 name→value)
- **문제 — 클라이언트가 15개+ 타입 키를 알아야 함**:
  ```json
  // 세일즈맵: 타입마다 다른 값 키
  { "fieldList": [
      { "name": "담당자",   "userValueId": "uuid" },
      { "name": "금액",     "numberValue": 50000 },
      { "name": "이메일",   "stringValue": "a@b.com" },
      { "name": "참여자",   "userValueIdList": ["uuid1","uuid2"] },
      { "name": "소속팀",   "teamValueIdList": ["team-uuid"] }
  ]}
  // HubSpot: 평탄한 name→value, 타입 키 없음
  { "properties": { "dealname":"New deal", "amount":"1500.00",
    "closedate":"2019-12-07T16:50:06.678Z", "hubspot_owner_id":"910901",
    "hs_buying_role":";BUDGET_HOLDER;END_USER" } }
  ```
- **실사용 실패 사례**: 사용자가 `{ "부가 서비스": "A" }`로 요청 — '부가 서비스'가 **복수선택(multiSelect)**이라 `{ "name":"부가 서비스", "stringValueList":["A"] }`(배열)여야 함. AI는 필드가 multiSelect인지 모르니 단일 문자열로 보내고, API는 `"부가 서비스에 stringValueList가 없습니다"`로 **거부**. HubSpot이었다면 그냥 통과했을 입력
- **근거 — API 주 소비자가 AI 에이전트가 된 시대엔 이 정도는 서버가 흡수하는 게 맞는 설계**: HubSpot은 타입별 부담이 "키 선택"이 아니라 **"값 표기 규칙"**(날짜=ISO 8601 또는 epoch ms, 복수선택=세미콜론 문자열)으로만 남고, 그마저 관대함 — **복수선택 필드에 단일 문자열을 보내도 400이 아니라 단일 값으로 수용**. 클라이언트(LLM)가 타입을 몰라도 대부분 통과하는 구조 (2026-07 공식 문서 재검증)
- **현재 우회와 비용**: `resolveProperties()`가 매번 `/v2/field/{type}`로 스키마(필드 타입)를 조회한 뒤 타입별 값 키로 변환 — `{ "부가 서비스":"A" }` → `{ "name":"부가 서비스", "stringValueList":["A"] }` (multiSelect→stringValueList 등 매핑 + 리스트 타입은 단일값을 배열로 자동 래핑). AI가 타입을 몰라도 자연값만 넘기게 흡수하는 게 핵심. 다만 **매 쓰기마다 스키마 조회 콜 1회 추가**되고, 이 흡수는 **MCP를 거칠 때만** — 공식 MCP·직접 API 사용자에겐 원래 부담이 그대로 노출. 필드 조회로 커버 가능하고 지금 잘 동작 중이라 P2
- **기대 효과**: 쓰기 전 스키마 조회 제거, 타입 불일치 에러 소멸, 클라이언트 구현 단순화. 뉴버전 API의 기본 설계 원칙(#3 위치 통일과 한 묶음)으로 삼을 것

### 그 외 P2

| 항목 | 원장 | 요청 | 우회 비용 |
|---|---|---|---|
| Search 응답 필드 선택 | #4-3 | search에 `properties[]` 지원 | search→batch-read **2단 호출(N+1 패턴)** 강제 |

## P3 — 우회되고 비효율도 거의 없음 (뉴버전 정리 대상)

| 항목 | 원장 | 요청 | 비고 |
|---|---|---|---|
| 노트 생성 메타 지정 | #12 | 전용 노트 생성 API (날짜·유형·담당자) | update의 memo로 생성은 됨, 메타만 미지정 |
| Association 대량 생성 전용 API | #20 | (선택) 연결만 대량 처리하는 엔드포인트 | update의 관계 필드로 우회 가능 — batch update가 관계 필드 수용하면 자동 커버, 별도 API 불필요할 수 있음 |
| 429 표준 헤더 | #28 부수 | `Retry-After` 헤더 | body 텍스트 파싱으로 대응 완료 |

---

## 참고: 이미 해결된 것

원장 하단 "해결됨 (메모)" 참조 — Batch Read(v3)·activity 유형별 조회·definition 목록·필드 생성 등 7건 (2026-06).
